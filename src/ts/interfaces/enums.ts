export enum Operations {
  MULTIPLY = '*',
  DIVIDE = '÷',
  PLUS = '+',
  MINUS = '-',
  EQUALS = '=',
}
export enum Brackets {
  LEFT = '(',
  RIGHT = ')',
}
export enum SequenceItems {
  NUMBER = 'number',
  OPERATION = 'operation',
  BRACKET = 'bracket',
}
